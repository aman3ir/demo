name: Publish to Exchange & Deploy to CH2.0

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-maven-

      - name: Set up JDK 1.8
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 8

      - name: Create Maven settings.xml with direct credentials
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml <<EOF
          <settings>
            <pluginGroups>
              <pluginGroup>org.mule.tools</pluginGroup>
              <pluginGroup>org.mule.tools.maven</pluginGroup>
            </pluginGroups>
            <servers>
              <server>
                <id>Repository</id>
                <username>cb967acbde4e45b4a019b0bba7cd055a</username>         <!-- Replace here -->
                <password>D1411F082eBD49e8807f1593Ed8FC62B</password>     <!-- Replace here -->
              </server>
              <server>
                <id>anypoint-exchange</id>
                <username>cb967acbde4e45b4a019b0bba7cd055a</username>         <!-- Replace here -->
                <password>D1411F082eBD49e8807f1593Ed8FC62B</password>     <!-- Replace here -->
              </server>
            </servers>
          </settings>
          EOF
        shell: bash

      - name: Publish to Exchange
        run: mvn clean deploy

      - name: Deploy to CloudHub 2.0
        run: >
          mvn clean deploy -DmuleDeploy
          -Danypoint.username=cb967acbde4e45b4a019b0bba7cd055a
          -Danypoint.password=D1411F082eBD49e8807f1593Ed8FC62B
